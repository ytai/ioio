package ioio.util;

import ioio.lib.PwmOutput;

/**
 * Represent a servo connected to the Ioio interface.
 * @author arshan
 */
public class Servo  {

    PwmOutput data_signal;
    
    public Servo(int pin) {

        // ioio.setDefaults(new HashMap{}());
       //  ioio.openSpiMaster(MISO, MOSI, CLK);
       // SpiChannel channel = spi_master.getConnection(CS)
    }
}
