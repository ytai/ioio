apply plugin: 'com.android.application'

android {
    compileSdkVersion 30
    defaultConfig {
        minSdkVersion 14
        targetSdkVersion 30

        versionCode getGitCommitCount()
        versionName getTag()
    }

    flavorDimensions "integration"
    productFlavors {
        code {
            dimension "integration"
        }
        library {
            dimension "integration"
        }
    }

}

dependencies {
    codeImplementation project(':IOIOLibAndroidBluetooth')
    codeImplementation project(':IOIOLibAndroidAccessory')
    codeImplementation project(':IOIOLibAndroidDevice')

    libraryImplementation 'com.github.ytai.ioio:IOIOLibAndroid:5.07' // the old artifacts still need it
    libraryImplementation 'com.github.ytai.ioio:IOIOLibAndroidBluetooth:5.07'
    libraryImplementation 'com.github.ytai.ioio:IOIOLibAndroidAccessory:5.07'
    libraryImplementation 'com.github.ytai.ioio:IOIOLibAndroidDevice:5.07'
}
